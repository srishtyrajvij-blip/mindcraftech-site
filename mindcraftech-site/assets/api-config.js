window.MINDCRAFTECH_API = {
  // Keep empty by default for static hosting (Hostinger, Netlify, etc.).
  // Set real absolute URLs when your backend endpoints are ready.
  contactEndpoint: "",
  assessmentEndpoint: ""
};
